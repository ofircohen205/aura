apiVersion: v1
kind: ConfigMap
metadata:
  name: web-dashboard-config
  labels:
    app: web-dashboard
    component: frontend
data:
  NODE_ENV: "development"
  NEXT_PUBLIC_API_URL: "http://backend:8000"
