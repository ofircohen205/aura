apiVersion: v1
kind: Service
metadata:
  name: web-dashboard
  labels:
    app: web-dashboard
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: web-dashboard
    component: frontend
