apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-dashboard
spec:
  replicas: 2 # Staging: 2 replicas
  template:
    spec:
      containers:
        - name: web-dashboard
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: 1000m
              memory: 1Gi
