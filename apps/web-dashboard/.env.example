# Aura Web Dashboard Environment Configuration Example
# Copy this file to .env.local, .env.development, or .env.production
# Next.js automatically loads the appropriate file based on NODE_ENV

# ============================================================================
# API Configuration
# ============================================================================
# Required: Backend API URL for the frontend to connect to
# This is exposed to the browser (NEXT_PUBLIC_ prefix)
# Default: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# Development Configuration
# ============================================================================
# Node environment (automatically set by Next.js, but can be overridden)
# Options: development, production, test
# NODE_ENV=development

# ============================================================================
# Docker Build Configuration
# ============================================================================
# Set to "true" when building for Docker (enables standalone output)
# Used in next.config.js
# DOCKER_BUILD=false

# ============================================================================
# Testing Configuration
# ============================================================================
# Base URL for Playwright E2E tests
# Default: http://localhost:3000
# PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# ============================================================================
# Notes
# ============================================================================
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Never commit secrets or API keys in environment variables
# 3. For production, set environment variables via your hosting platform
#    (e.g., Vercel, Docker, Kubernetes)
# 4. Next.js automatically loads:
#    - .env.local (loaded in all environments, ignored by git)
#    - .env.development (loaded in development)
#    - .env.production (loaded in production)
#    - .env (loaded in all environments, should not be used)
